<script setup>
import Modal from './Modal.vue';

const emit = defineEmits(['close']);

defineProps({
    show: {
        type: Boolean,
        default: false,
    },
    maxWidth: {
        type: String,
        default: '2xl',
    },
    closeable: {
        type: Boolean,
        default: true,
    },
});

const close = () => {
    emit('close');
};
</script>

<template>
    <Modal
        :show="show"
        :max-width="maxWidth"
        :closeable="closeable"
        @close="close"
    >
        <div class="px-6 py-4">
            <div class="text-[17px] font-bold text-t1">
                <slot name="title" />
            </div>

            <div class="mt-4 text-[13.5px] leading-relaxed text-t2">
                <slot name="content" />
            </div>
        </div>

        <div class="flex flex-row justify-end gap-2.5 border-t px-6 py-4 text-end" style="background: var(--s2); border-color: var(--line)">
            <slot name="footer" />
        </div>
    </Modal>
</template>
